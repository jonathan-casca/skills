# Admin And Support Tooling Prompts

```text
Identify the internal admin tools this app needs before production. Include user lookup, tenant/org inspection, job retry, webhook replay, audit logs, feature flags, and manual override workflows.
```

```text
Design a support dashboard for debugging customer issues without direct database access. List the screens, actions, permissions, and audit logs required.
```

```text
Find workflows where support would currently need to manually edit the database. Propose safe admin actions instead.
```

```text
Create an audit logging plan for important user, admin, and system actions.
```
